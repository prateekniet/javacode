name: Java CI with Maven
on:
 push:
  branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
     DB_PASSWORD: ${{ secrets.DB_PASS }}
    steps:
      - name: Print variable
        run: echo $DB_PASSWORD
